<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Base.Theme.ChatbotApp" parent="Theme.Material3.DayNight.NoActionBar">

        <!-- Primary — amber -->
        <item name="colorPrimary">@color/amber_primary</item>
        <item name="colorOnPrimary">@color/text_on_amber</item>
        <item name="colorPrimaryContainer">@color/amber_subtle</item>
        <item name="colorOnPrimaryContainer">@color/amber_light</item>

        <!-- Secondary — darker amber -->
        <item name="colorSecondary">@color/amber_dark</item>
        <item name="colorOnSecondary">@color/text_cream</item>
        <item name="colorSecondaryContainer">@color/amber_subtle</item>
        <item name="colorOnSecondaryContainer">@color/amber_light</item>

        <!-- Surface -->
        <item name="colorSurface">@color/bg_surface</item>
        <item name="colorOnSurface">@color/text_cream</item>

        <!-- Background (android: prefix = system attribute, always valid) -->
        <item name="android:colorBackground">@color/bg_base</item>

        <!-- Error -->
        <item name="colorError">@color/color_error</item>
        <item name="colorOnError">@color/text_cream</item>

        <!-- System bars -->
        <item name="android:statusBarColor">@color/bg_deep</item>
        <item name="android:navigationBarColor">@color/bg_deep</item>
        <item name="android:windowLightStatusBar" tools:targetApi="23">false</item>

    </style>

    <style name="Theme.ChatbotApp" parent="Base.Theme.ChatbotApp" />
</resources>
